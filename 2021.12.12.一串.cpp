# include<stdio.h>
# include<string.h>
int main(void)
{
   int a[20],len,i,s;
   char str[100];
   printf("input string:\n");
   gets(str);
   len=strlen(str);
   for(i=0;i<len;i++)
   {
       if(str[i]>='0'&&str[i]<='9')
	   {
	       s=str[i]-'0';
	   }
	   for(j=i+1;j<)
   }
} 